<% @items.each do |item| %>
  <div style="float:left; margin:20px 20px 10px 20px; width:200px;
  ">
  <a href='<%= item.url %>'>
    <img src='<%= item.image_url %>' height="200" />
  </a> <br/>
  <%= item.description %> | <%= item.price %>
  </div>

<% end %>  
